import 'package:flutter/material.dart';

class Constants {
  static BoxDecoration cardDecoration = BoxDecoration(
      color: Colors.white, border: Border.all(width: 1, color: Colors.black12), borderRadius: BorderRadius.circular(2));
}
